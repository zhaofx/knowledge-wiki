# ==============================================================================
# Knowledge Wiki 环境变量模板
# ------------------------------------------------------------------------------
# 使用方式：
#   1. 复制本文件为 `.env`：`cp .env.example .env`
#   2. 按本机实际情况填写下面的变量
#   3. `.env` 已在 `.gitignore` 中忽略，不会被提交到版本库
#
# 加载方式：
#   - Shell 会话：`set -a && source .env && set +a`
#   - Agent 宿主：多数宿主（Claude Code / Cursor / Mira）在启动时自动读取仓库根
#     目录下的 `.env`；若未自动加载，请在宿主设置中手动指定。
#   - Skill 引用：所有 `skills/*/SKILL.md` 通过 `${KNOWLEDGE_ROOT_DIR}/knowledge/...`
#     形式引用工作区路径，宿主在装载 Skill 时会将变量原地展开。
# ==============================================================================


# ------------------------------------------------------------------------------
# 【必填】KNOWLEDGE_ROOT_DIR —— 知识库根目录的绝对路径
# ------------------------------------------------------------------------------
# 作用：
#   Skill 定位工作区的唯一锚点。所有七个 knowledge-wiki-* Skill 都会以
#   `${KNOWLEDGE_ROOT_DIR}/knowledge/AIDE.md` 读取硬规则，以
#   `${KNOWLEDGE_ROOT_DIR}/knowledge/{raw,wiki,output}/` 定位读写目录。
#
# 要求：
#   - 必须是【绝对路径】（不要用 `~` 或相对路径），否则 Skill 内的路径拼接会失败
#   - 指向本仓库的根目录（即包含 `knowledge/`、`skills/`、`scripts/` 的那一层）
#   - 路径末尾不要带斜杠
#
# 示例：
#   macOS   : KNOWLEDGE_ROOT_DIR=/Users/you/git/knowledge-wiki
#   Linux   : KNOWLEDGE_ROOT_DIR=/home/you/git/knowledge-wiki
#   WSL     : KNOWLEDGE_ROOT_DIR=/mnt/c/Users/you/git/knowledge-wiki
KNOWLEDGE_ROOT_DIR=


# ==============================================================================
# 以下为【可选】变量：仅在需要调用对应远端知识能力时才启用（取消注释并填值）。
# 未启用时，相关 Skill / CLI 将回退到"只用本地 raw/wiki"的降级模式，不会阻断
# 主流程。
# ==============================================================================


# ------------------------------------------------------------------------------
# 飞书 / Lark 应用凭证
# ------------------------------------------------------------------------------
# 作用：
#   当使用 lark-* 系列 skill（lark-doc / lark-sheets / lark-base / lark-wiki
#   / lark-drive / lark-im 等）或 knowledge-wiki-ingest 摄取飞书 URL 时，
#   底层 `lark-cli` 需要企业自建应用的 App ID / App Secret 换取 tenant token。
#
# 获取方式：
#   1. 在飞书开放平台 https://open.feishu.cn/ 创建"企业自建应用"
#   2. 在"凭证与基础信息"页面复制 App ID 和 App Secret
#   3. 按需申请以下权限（对应本项目常见摄取场景）：
#        - wiki:wiki                     读取知识库空间与节点
#        - docx:document                 读写云文档正文
#        - docx:document.content:read    读取文档结构化内容
#        - drive:drive / drive:file      读写云空间文件
#        - bitable:app                   读取多维表格
#        - sheets:spreadsheet            读取电子表格
#   4. 首次使用需执行 `lark-cli auth login` 完成登录授权
#
# 注意：这两个值是【机密】，请勿提交到 git，也不要贴到公开渠道。
# LARK_APP_ID=cli_xxxxxxxxxxxxxxxx
# LARK_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# ------------------------------------------------------------------------------
# ByteCloud 数据库工具箱访问凭证
# ------------------------------------------------------------------------------
# 作用：
#   当使用 TRAE-database-toolbox skill 连接字节云 ByteRDS / ByteRedis /
#   ByteDoc，执行库表查询、慢查询诊断、锁分析、数据画像、工单提交等操作时，
#   工具会以此 token 调用 ByteCloud 平台 API。
#
# 获取方式：
#   在字节内网 ByteCloud 控制台 → 个人中心 → API Token 页面申请。
#   Token 具备账号级别权限，泄露后请立即在同一页面吊销。
#
# BYTECLOUD_TOKEN=


# ------------------------------------------------------------------------------
# （示例扩展位）其他远端知识能力凭证
# ------------------------------------------------------------------------------
# 如果你后续接入了自研或第三方远端知识工具（Aeolus / Nuwa / 风神 / Dorado
# 等），建议按以下格式补充说明并追加变量，方便团队理解：
#
#   # <平台名> —— <一句话作用>
#   # 获取方式：<内部文档 URL 或步骤>
#   # <PLATFORM>_TOKEN=
